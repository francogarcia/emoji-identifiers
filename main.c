#include <stdio.h>
#define 🖨️(X) printf("%d\n", (X))
#define 🧮(X) ((X) * (X))
#define ²
#define 🏁
#define 👋 return 0

void 👀(unsigned char 🕶️) {
    if (!🕶️) {
        return;
    }

    🖨️(🕶️);

    👀(--🕶️);
}

int main()🏁 {
    const int 💯 = 100;
    int 🖩 = 💯 + 💯;
    🖨️(🖩);

    🖩 = 🧮(💯)²;
    🖨️(🖩);

    👀(💯 / 💯);

    👋;
}
